<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="@dimen/fc_padding_extra_small"
    app:cardCornerRadius="@dimen/fc_corner_radius"
    app:cardElevation="@dimen/fc_elevation_small"
    app:cardBackgroundColor="@color/fc_card_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/fc_padding_medium">

        <TextView
            android:id="@+id/text_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/fc_text_subtitle"
            android:textColor="@color/fc_text_primary"
            android:textStyle="bold"
            android:maxLines="2"
            android:ellipsize="end"
            tools:text="How to grow tomatoes in winter" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="@dimen/fc_padding_small">

            <TextView
                android:id="@+id/text_date"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="@dimen/fc_text_caption"
                android:textColor="@color/fc_text_secondary"
                tools:text="Mar 25, 2026" />

            <TextView
                android:id="@+id/text_message_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/fc_text_caption"
                android:textColor="@color/fc_text_secondary"
                tools:text="12 messages" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
