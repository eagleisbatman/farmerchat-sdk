package org.digitalgreen.farmerchat.compose.theme

import androidx.compose.ui.graphics.Color

// ── Brand greens ─────────────────────────────────────────────────────────────
val SdkGreen800   = Color(0xFF2E7D32) // PRIMARY_GREEN
val SdkGreen600   = Color(0xFF43A047)
val SdkGreen500   = Color(0xFF4CAF50) // LIGHT_GREEN — user bubbles, FAB, send btn
val SdkGreen400   = Color(0xFF66BB6A)
val SdkGreen300   = Color(0xFF81C784)
val SdkGreenAccent = Color(0xFF69F0AE) // online dot

// ── Dark surfaces ─────────────────────────────────────────────────────────────
val SdkDarkBg        = Color(0xFF0F1A0D) // deepest background
val SdkDarkSurface   = Color(0xFF1A2318) // card, AI bubble, input bar
val SdkDarkSurface2  = Color(0xFF243020) // elevated card, text field
val SdkDarkSurface3  = Color(0xFF2C3B28) // highest elevation

// ── Dark text ─────────────────────────────────────────────────────────────────
val SdkTextPrimary   = Color(0xFFE8F5E9) // near-white green tint
val SdkTextSecondary = Color(0xFF8FA88C) // secondary / muted
val SdkTextMuted     = Color(0xFF5A6B58) // timestamps, very muted

// ── Chat bubbles ──────────────────────────────────────────────────────────────
val SdkUserBubble     = Color(0xFF4CAF50) // user bubble bg
val SdkUserBubbleText = Color(0xFFFFFFFF)
val SdkAiBubble       = Color(0xFF1A2318) // AI bubble bg
val SdkAiBubbleText   = Color(0xFFE8F5E9)

// ── Error ─────────────────────────────────────────────────────────────────────
val SdkError          = Color(0xFFCF6679)
val SdkErrorContainer = Color(0xFF3D1C22)

// ── History ───────────────────────────────────────────────────────────────────
val HistoryBackground = Color(0xFF0D1A0B)
val HistoryCardBg     = Color(0xFF172213)
val HistoryGroupLabel = Color(0xFF4A5E48)

// ── Misc ──────────────────────────────────────────────────────────────────────
val SdkPrimaryContainer = Color(0xFF1F3A1D)
val SdkOutlineVariant   = Color(0xFF2E3F2C)
